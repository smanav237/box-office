import { useState } from "react";

const SearchForm = ({onSearch}) =>{     // onSearch fn from Home.jsx

    const [searchStr, setSearchStr] = useState("");
    const [searchOption,setSearchOption] = useState('shows');
}